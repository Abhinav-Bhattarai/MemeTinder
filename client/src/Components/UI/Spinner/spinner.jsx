import React, { Fragment } from 'react';
import './spinner.scss';

const Spinner = () => {
    return (
        <Fragment>
            <div className="loader">Loading...</div>
        </Fragment>
    )
}

export default Spinner