import React, { Fragment } from 'react';
import './request-card.scss';

const RequestCard = ({ sender, profile_picture }) => {
    return (
        <Fragment>

        </Fragment>
    )
}

export default RequestCard;
